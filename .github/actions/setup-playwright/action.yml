name: "Setup Playwright"
description: "Install Playwright browsers and OS dependencies with browser cache"
runs:
  using: "composite"
  steps:
    - name: Get Playwright version
      id: playwright-version
      shell: bash
      run: echo "version=$(npx playwright --version 2>/dev/null || echo 'unknown')" >> "$GITHUB_OUTPUT"

    - name: Cache Playwright browsers
      if: github.ref == 'refs/heads/main'
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

    - name: Restore Playwright browsers
      if: github.ref != 'refs/heads/main'
      uses: actions/cache/restore@v5
      id: playwright-cache-restore
      with:
        path: ~/.cache/ms-playwright
        key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

    - name: Install Playwright browsers
      if: steps.playwright-cache.outputs.cache-hit != 'true' && steps.playwright-cache-restore.outputs.cache-hit != 'true'
      shell: bash
      run: npx playwright install --with-deps

    - name: Install Playwright OS dependencies only
      if: steps.playwright-cache.outputs.cache-hit == 'true' || steps.playwright-cache-restore.outputs.cache-hit == 'true'
      shell: bash
      run: npx playwright install-deps
