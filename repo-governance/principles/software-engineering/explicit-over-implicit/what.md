---
description: Contrasts explicit and implicit configuration.
when_to_use: Use for a quick working definition of explicit vs. implicit behaviour.
---

# What

**Explicit configuration** means:

- All behaviour is visible and stated clearly
- No hidden defaults or "magic" conventions
- Configuration is written out, not inferred
- Dependencies are declared, not assumed
- Behaviour is predictable and transparent

**Implicit configuration** means:

- Behaviour relies on defaults or conventions
- Configuration is inferred from context
- "Magic" happens behind the scenes
- Dependencies are assumed or auto-discovered
- Behaviour requires insider knowledge
