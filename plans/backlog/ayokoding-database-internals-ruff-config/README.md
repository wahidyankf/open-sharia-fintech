# Database Internals Course Ruff Configuration

Add the missing course-scoped `ruff.toml` for
`database-internals-and-storage-engines`, matching the established configuration for code-bearing
courses while preserving its annotated examples and drills.

## Scope

- Add the course-local Ruff configuration only.
- Add the required companion specs and regression coverage before any application-code change lands.
- Do not alter course prose or unrelated formatting.

## Delivery

- **Delivery Mode**: `worktree-to-pr`.
- **Blocks**: none.
- **Blocked by**: none.

This backlog item exists because the change is code-homed and therefore cannot be made inline while
closing `ayokoding-learning-path-04-course-authoring`.
