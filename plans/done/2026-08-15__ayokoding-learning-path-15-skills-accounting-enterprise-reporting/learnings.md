<!-- Knowledge Capture running log — append entries during execution. -->
<!-- Triage every entry (or record the explicit "none" escape) before archival. -->

# Learnings: ayokoding-learning-path-15-skills-accounting-enterprise-reporting

No generalizable learnings — the temporary generated `.next` cleanup is an environment artifact, while manifest integrity, behavior coverage, and affected gates already catch durable source regressions.
