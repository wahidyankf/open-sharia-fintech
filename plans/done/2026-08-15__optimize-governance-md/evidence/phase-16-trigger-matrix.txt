Phase 16b-2 — 5-case trigger matrix (ose-private mirror armed in Phase 16, re-verified here
against the real `ose-public` rhino-cli binary since Phase 16's original evidence write did not
land in this checkout's evidence/ folder before archival — recreated 2026-08-15 using the same
isolated no-origin git fixture method established at Phase 9b-2).

Method: `git init -q` in a scratch dir with no `origin` remote (so `merge_base_paths()`'s
`git merge-base origin/main HEAD` fails and the runner falls back to `staged_paths()` — the
mechanism documented in `apps/rhino-cli/src/commands/gate/run.rs`), a minimal `repo-config.yml`
declaring `governance-word-budget` and `governance-readme-completeness` on both `pre-push` and
`ci` surfaces with the same trigger globs as the real repos, then per case: `git add` the
triggering file (STAGED, not committed — the fallback path reads staged, not committed, diffs),
run `gate run --surface=ci --only=<gate-id>`, grep for "Running gate <id>" (executed) vs. its
absence (silently skipped), then commit and move to the next case.

=== Case 1: touch repo-governance/README.md (expect: both EXECUTE) ===
  governance-word-budget: EXECUTED
  governance-readme-completeness: EXECUTED

=== Case 2: touch apps/ayokoding-www/content (expect: both SKIP) ===
  governance-word-budget: SKIPPED (silent)
  governance-readme-completeness: SKIPPED (silent)

=== Case 3: touch .opencode/agents mirror (expect: word-budget EXECUTE, readme-completeness SKIP) ===
  governance-word-budget: EXECUTED
  governance-readme-completeness: SKIPPED (silent)

=== Case 4: touch plans/done/.../delivery.md (expect: both SKIP) ===
  governance-word-budget: SKIPPED (silent)
  governance-readme-completeness: SKIPPED (silent)

=== Case 5: touch repo-config.yml (expect: both SKIP — not in either gate's trigger list) ===
  governance-word-budget: SKIPPED (silent)
  governance-readme-completeness: SKIPPED (silent)

Result: all 5 outcomes match the Phase 9b-2 reference pattern exactly. Both gates' `ci`-surface
`path-gated` declarations fire correctly and skip correctly — the same conclusion independently
corroborated by PR16 (#49)'s real CI run, where the `governance` job (which runs these same gates
on the `ci` surface against the real merge-base diff) passed green on a diff that legitimately
touched `repo-governance/` content.
