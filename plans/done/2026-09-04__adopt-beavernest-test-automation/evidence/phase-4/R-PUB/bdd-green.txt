GREEN — exact BDD (D-P4-PUB-BDD)
branch: test-contract-validator-foundation-bdd
command: rtk nx run rhino-cli:test:unit --skip-nx-cache

Result summary
Passed!  - Failed:     0, Passed:  1578, Skipped:     0, Total:  1578, Duration: 1 m 39 s - RhinoCli.UnitTests.dll (net10.0)

Delta against RED (same command, same fixtures)
  RED:   Failed: 54, Passed: 1524, Total: 1578
  GREEN: Failed:  0, Passed: 1578, Total: 1578
  Every one of the 54 RED failures is a TestContractBddUnitTests case; no other
  suite changed count, so nothing was weakened to reach green.

Each negative fixture, its first-class diagnostic, and the exact integer pair it pins
fixture                 diagnostic                        pinned counts
missing-feature         bdd-uncovered-feature             files=1/2
missing-scenario        bdd-uncovered-scenario            scenarios=1/2
missing-example         bdd-uncovered-example             examples=1/2
missing-step            bdd-missing-required-keyword      keyword=When
missing-binding         bdd-undefined-binding             candidates=0, steps=2/3
missing-owner-adapter   bdd-uncovered-owner-adapter       item=widget-app@unit, pairs=0/1
unused-binding          bdd-unused-binding                item names the unmatched binding
duplicate-binding       bdd-ambiguous-binding             candidates=2
rounded-999-of-1000     bdd-uncovered-scenario            scenarios=999/1000, no percentage rendered

Every failure line carries project, owner, adapter, item, covered, total, and remediation;
every failure ends with a behavior-coverage-failed line naming all five categories.
The gate compares integers only: no percentage is emitted on the success or failure path.

Exact positives
  single feature, 3 steps, fully bound        -> files=1/1 examples=1/1 scenarios=1/1 steps=3/3 pairs=1/1
  two features, fully bound                   -> files=2/2 steps=6/6
  scenario outline, 3 examples, fully bound   -> scenarios=1/1 examples=3/3 steps=9/9
  delegated adapter, fully bound              -> pairs=1/1
  inapplicable adapter                        -> behavior-coverage-not-applicable, no denominator
  synthetic 1000/1000                         -> scenarios=1000/1000

Nx target surface added to apps/rhino-cli/project.json
  test:behavior:coverage:unit
    command: dotnet run --project apps/rhino-cli/src/RhinoCli.Program/RhinoCli.Program.fsproj -- test-contract behavior-coverage validate --project rhino-cli --adapter unit
  test:behavior:coverage:integration
    command: dotnet run --project apps/rhino-cli/src/RhinoCli.Program/RhinoCli.Program.fsproj -- test-contract behavior-coverage validate --project rhino-cli --adapter integration
  test:behavior:coverage:e2e
    command: dotnet run --project apps/rhino-cli/src/RhinoCli.Program/RhinoCli.Program.fsproj -- test-contract behavior-coverage validate --project rhino-cli --adapter e2e
  test:behavior:coverage
    dependsOn: test:behavior:coverage:unit, test:behavior:coverage:integration, test:behavior:coverage:e2e

Deliberate scope note — the targets are declared, not yet wired into a gate.
  The frozen Phase 0 allocation assigns apps/rhino-cli/src/RhinoCli.Cli/src/Dispatch.fs and
  HelpText.fs to D-P4-PUB-REGISTRY alone, so this leaf cannot add the
  'test-contract behavior-coverage validate' route; invoking one of the four targets today
  exits 2 with the CLI's unrouted-invocation message. They are therefore excluded from
  rhino-cli:test:quick and from every CI target list (CI runs specs:behavior:coverage, a
  different and already-routed target), so nothing regresses and nothing is falsely green.
  rhino-cli's own registry row also declares unit and integration drivers at
  apps/rhino-cli/tests/{unit,integration}/Behavior/RhinoCliBehaviorDriver.fs, which do not
  exist yet; those arrive with the owner delivery that moves the executable tests.
