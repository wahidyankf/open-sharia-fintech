# 47 · Advanced Frontend (By Example, TypeScript †)

**prd row**: Pass 3 · Build for the Real World · By Example · TypeScript † · Learn 147 / Drill 247 ·
Nvim-ready Yes · VSCode-ready Yes. ([prd canonical table](../prd.md#the-94-topics--canonical-table-spiral-order-identical-in-both-tracks))

**Scope note**: the deep frontend pass — performance (Core Web Vitals), rendering strategies
(CSR/SSR/SSG/streaming), state-at-scale, advanced accessibility (ARIA/focus/live regions), and applied
component + e2e testing from the CLI. The usable slice is the prerequisite
[`14-frontend-essentials`](./14-frontend-essentials.md).

## Why this exists · the big idea

- **The problem before the solution**: a UI that renders correctly can still fail its users — slow to paint,
  janky to interact, inaccessible to a screen reader, and impossible to reason about once state sprawls.
- **Keep-this-if-you-forget-everything**: the frontend's hard problems are state and time — derive UI from
  state, push data-fetching and caching to well-defined edges, and _measure_ performance instead of guessing.
- **Big ideas touched**: `taming-state` (state-at-scale is the central difficulty),
  `consistency-latency-throughput` (Core Web Vitals and rendering strategy are latency/throughput trade-offs).

## Prerequisites

- **Prior topics**: [topic 14 Frontend Essentials](./14-frontend-essentials.md) (components, state,
  accessible forms), [topic 13 Just Enough TypeScript](./13-just-enough-typescript.md), and
  [topic 15 Software Testing](./15-software-testing.md) (Testing-Library + e2e).
- **Tools & environment**: a macOS/Linux terminal; **Node.js** + a pinned CVE-clean UI framework + build
  tool; **Vitest** + Testing-Library; **Playwright** for an e2e smoke; CLI **Lighthouse** for perf
  measurement; a browser.
- **Assumed knowledge**: building a typed component with state + an accessible form (topic 14); running a
  component test from the CLI (topic 15).

## Accuracy notes (web-verified)

> Verified in the pre-authoring `web-researcher` sweep (#48, DD-28).

- 2026-07-12 — verified: Core Web Vitals thresholds current for 2026 — **LCP** good ≤2.5s (poor >4.0s),
  **INP** good <200ms (poor >500ms), **CLS** good <0.1 (poor >0.25), at the 75th percentile over a rolling
  28-day CrUX window. **INP replaced FID** (March 2024) — the syllabus's LCP/INP/CLS triad (not FID) is
  correctly up to date. Playwright/Lighthouse CLI + ARIA practices actively evolve — spot-check concrete
  CLI invocations at authoring. (developers.google.com/search/docs/appearance/core-web-vitals)

> DD-35 primary-source pass (2026-07-12). Thresholds, directive names, and API semantics traced to
> official docs (react.dev, web.dev, w3.org, MDN, TypeScript Handbook, Testing Library, Astro) and
> fetched/read. Fast-moving framework specifics carry version notes; unverifiable items flagged.

- **Core Web Vitals** — **INP replaced FID** as a stable Core Web Vital on **March 12, 2024**
  ([web.dev, INP CWV launch](https://web.dev/blog/inp-cwv-launch)). **LCP**: good ≤ 2.5 s / poor > 4.0 s;
  **INP**: good ≤ 200 ms / poor > 500 ms; **CLS**: good ≤ 0.1 / poor > 0.25 — all at the 75th percentile.
  CLS = "the largest burst of layout shift scores … during the entire lifecycle of a page." Sources:
  [LCP](https://web.dev/articles/lcp), [INP](https://web.dev/articles/inp), [CLS](https://web.dev/articles/cls) (all fetched, verbatim thresholds).
- **React Server Components** — `'use client'` "must be at the very beginning of a file, above any imports";
  it "defines the boundary between server and client code on the module dependency tree, **not** the render
  tree" (a client component can render a server component via `children`). Source: [react.dev, 'use client'](https://react.dev/reference/rsc/use-client) (fetched, verbatim).
- **Hydration** — "`hydrateRoot` lets you display React components inside a browser DOM node whose HTML
  content was previously generated by `react-dom/server`." Source: [react.dev, hydrateRoot](https://react.dev/reference/react-dom/client/hydrateRoot) (fetched).
- **Islands architecture** — "rendering the majority of your page to fast, static HTML with smaller
  'islands' of JavaScript … when interactivity … is needed." Term coined by Katie Sylor-Miller (2019),
  expanded by Jason Miller (2020). Source: [Astro Docs — Islands](https://docs.astro.build/en/concepts/islands/) (fetched). "Progressive hydration" is an ecosystem pattern name, not a spec term — `[Needs Verification]`.
- **Critical rendering path** — "CSS is treated as a render-blocking resource … the browser won't render
  any … content until the CSSOM is constructed"; "both the DOM and the CSSOM are required to construct the
  render tree." Source: [web.dev — render-blocking CSS](https://web.dev/articles/critical-rendering-path/render-blocking-css) (fetched).
- **Code splitting / tree shaking** — `React.lazy(load)` must be top-level and needs a `<Suspense>` wrapper
  ([react.dev, lazy](https://react.dev/reference/react/lazy)); `next/dynamic` "is a composite of `React.lazy()` and `Suspense`" ([Next.js lazy-loading](https://nextjs.org/docs/app/guides/lazy-loading)). Tree shaking = "the removal of dead code" via ES-module static analysis ([MDN](https://developer.mozilla.org/en-US/docs/Glossary/Tree_shaking)).
- **Rules of Hooks** — (1) only call Hooks at the top level (not in loops/conditions/nested functions/after
  an early return); (2) only call them from React function components or custom Hooks. Source: [react.dev, Rules of Hooks](https://react.dev/reference/rules/rules-of-hooks) (fetched). The _why_ (call-order-indexed per-fiber state) is community/Fiber knowledge, `[Needs Verification]` as an official-docs quote.
- **useEffect** — "Effects run at the end of a commit after the screen updates"; "React will first run the
  cleanup … with the old values, and then run your setup … with the new values"; dependencies compared with
  `Object.is`; no array → runs every commit, `[]` → mount only. Source: [react.dev, Synchronizing with Effects](https://react.dev/learn/synchronizing-with-effects) + [useEffect](https://react.dev/reference/react/useEffect) (fetched).
- **useMemo/useCallback** — "`useMemo` … lets you cache the result of a calculation between re-renders";
  `useCallback` is the function-specialized form. Source: [react.dev, useMemo](https://react.dev/reference/react/useMemo) (fetched, verbatim).
- **Reconciliation & keys** — "the state of the art algorithms have a complexity in the order of O(n³) …
  React implements a heuristic O(n) algorithm" on two assumptions: different-type elements produce different
  trees, and keys hint stable children; "unstable keys (like those produced by `Math.random()`) will cause
  many … DOM nodes to be unnecessarily recreated." Source: [Reconciliation (legacy.reactjs.org)](https://legacy.reactjs.org/docs/reconciliation.html) — note this is the **frozen legacy page** ("old and won't be updated"), still the only official algorithm explainer; current-docs framing is position-based identity ([react.dev, Preserving and Resetting State](https://react.dev/learn/preserving-and-resetting-state)).
- **Signals** — "a signal is an object with a `.value` property … the signal itself always stays the same";
  "computed signals track which signals are accessed and re-run … when those signals change." Source:
  [Preact Signals](https://preactjs.com/guide/v10/signals/) (fetched). SolidJS: "compiles its templates to real DOM nodes … only the code that depends on it will rerun" ([solidjs/solid README](https://github.com/solidjs/solid)); `docs.solidjs.com` is Cloudflare-blocked to fetch — verbatim SolidJS docs quotes `[Needs Verification]`.
- **WCAG 2.2** — a **W3C Recommendation on October 5, 2023** (the TR page's Dec 2024 date is a post-REC
  errata update). It adds **9 new Success Criteria** vs 2.1 (2.4.11/2.4.12/2.4.13 Focus, 2.5.7 Dragging,
  2.5.8 Target Size, 3.2.6 Consistent Help, 3.3.7 Redundant Entry, 3.3.8/3.3.9 Accessible Authentication).
  Sources: [W3C WAI news](https://www.w3.org/WAI/news/2023-10-05/wcag22rec/), [What's New in 2.2](https://www.w3.org/WAI/standards-guidelines/wcag/new-in-22/) (fetched).
- **Focus management** — "the focused element is the active element (`document.activeElement`)"; strategies
  are **roving tabindex** and **`aria-activedescendant`**. Source: [W3C WAI-ARIA APG — Keyboard Interface](https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/) (fetched).
- **Controlled vs uncontrolled inputs** — "An input like `<input />` is uncontrolled"; a controlled input
  gets a `value` (or `checked`) prop; "An input can't be both controlled and uncontrolled … [and] cannot
  switch … over its lifetime." Source: [react.dev, `<input>`](https://react.dev/reference/react-dom/components/input) (fetched, verbatim).
- **HTTP caching / SWR** — "stale-while-revalidate … indicates that the cache could reuse a stale response
  while it revalidates" (RFC 5861); example `Cache-Control: max-age=604800, stale-while-revalidate=86400`.
  Source: [MDN — Cache-Control](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control#stale-while-revalidate) (fetched).
- **CSS at scale** — "A CSS Module is a CSS file where all class names … are scoped locally by default"
  ([css-modules README](https://github.com/css-modules/css-modules)); Tailwind = "combining many
  single-purpose … utility classes directly in your markup" ([Tailwind docs](https://tailwindcss.com/docs/styling-with-utility-classes)); specificity is the **ID–CLASS–TYPE** three-column model, ties broken by source order ([MDN Specificity](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Specificity)).
- **Data fetching / Suspense** — "`<Suspense>` lets you display a fallback until its children have finished
  loading"; crucially "**Suspense does not detect when data is fetched inside an Effect or event handler**" —
  only `use()` of a promise, lazy code, and framework integrations trigger it. Prefetching = "loading a route
  in the background before the user navigates." Sources: [react.dev, `<Suspense>`](https://react.dev/reference/react/Suspense), [Next.js linking-and-navigating](https://nextjs.org/docs/app/getting-started/linking-and-navigating) (fetched).
- **Image optimization** — `next/image` `alt` became **required in v13.0.0**; `loading` **defaults to
  `"lazy"`** ("Defers loading the image until it reaches a calculated distance from the viewport") — a
  deliberate divergence from the native `<img loading>` default of **`eager`**. `srcset` uses `w`/`x`
  descriptors; `sizes` defaults to `100vw`. Sources: [Next.js Image](https://nextjs.org/docs/app/api-reference/components/image), [MDN `<img>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/img) (fetched).
- **Edge runtime** — `runtime: 'nodejs' | 'edge'`, default `'nodejs'`; the edge runtime is a V8-isolate
  subset of Web APIs (`fetch`/`Request`/`Response`), ES-modules only. Do not show the deprecated
  `"experimental-edge"` (deprecated v15.0.0-RC). Source: [Next.js Route Segment Config](https://nextjs.org/docs/app/api-reference/file-conventions/route-segment-config) (fetched).
- **TypeScript in frontend** — a discriminated union: "When every type in a union contains a common property
  with literal types, TypeScript … can narrow out the members of the union." Source: [TS Handbook — Narrowing](https://www.typescriptlang.org/docs/handbook/2/narrowing.html#discriminated-unions) (fetched, verbatim) — model `loading`/`error`/`success` state this way.
- **Testing** — "The more your tests resemble the way your software is used, the more confidence they can
  give you" ([Testing Library — Guiding Principles](https://testing-library.com/docs/guiding-principles/)); "Playwright Test is an end-to-end test framework for modern web apps" ([Playwright](https://playwright.dev/docs/intro)) (both fetched).

## Concepts

<!-- co-NN · concept enumeration (DD-34): every concept this topic teaches, 1:1-mirrored to a delivery.md checkbox. Floor ≥ 10 (By-Example subject topic). Each example below cites the co-NN it exercises. -->

- **co-01 · rendering-csr** — client-side rendering: the browser boots a JS bundle and renders into an empty shell.
- **co-02 · rendering-ssr** — server-side rendering: the server returns fully-formed HTML per request.
- **co-03 · rendering-ssg** — static site generation: pages pre-rendered to HTML at build time.
- **co-04 · streaming-ssr** — stream HTML in chunks with `<Suspense>` fallbacks as async work resolves.
- **co-05 · hydration** — attach React's logic to server-generated HTML via `hydrateRoot`.
- **co-06 · islands-architecture** — mostly static HTML with small independently-hydrated interactive islands (Astro).
- **co-07 · react-server-components** — server vs client components; `'use client'` marks the module-tree boundary.
- **co-08 · core-web-vitals** — LCP/INP/CLS as the user-experience triad (INP replaced FID in March 2024).
- **co-09 · lcp** — Largest Contentful Paint measures loading; good ≤ 2.5 s.
- **co-10 · inp** — Interaction to Next Paint measures responsiveness; good ≤ 200 ms.
- **co-11 · cls** — Cumulative Layout Shift measures visual stability; good ≤ 0.1.
- **co-12 · critical-rendering-path** — CSS is render-blocking; the render tree needs both DOM and CSSOM.
- **co-13 · code-splitting** — `React.lazy` + `<Suspense>` and route-based splitting defer non-critical code.
- **co-14 · tree-shaking** — dead-code elimination through static ES-module analysis.
- **co-15 · client-vs-server-state** — separate client UI state from server cache state (TanStack Query), with invalidation and optimistic updates.
- **co-16 · when-redux** — reach for a global store (Redux Toolkit) only for large, frequently-updated shared state.
- **co-17 · rules-of-hooks** — call Hooks only at the top level and only from React functions.
- **co-18 · useeffect-semantics** — effects run after paint, clean up before re-run/unmount, and re-run on `Object.is`-changed dependencies.
- **co-19 · usememo-usecallback** — cache an expensive value / a stable function reference between renders.
- **co-20 · reconciliation-keys** — React identifies elements by tree position; keys give list items stable identity.
- **co-21 · virtual-dom** — diff a virtual node tree and patch only the changed real DOM (O(n) heuristic).
- **co-22 · signals-fine-grained** — a `.value` signal with automatic dependency tracking updates only its dependents, no full re-render (Preact/Solid).
- **co-23 · wcag-accessibility** — WCAG 2.2 (9 new success criteria), semantic HTML, ARIA roles, and live regions.
- **co-24 · focus-management** — `document.activeElement`, roving tabindex, `aria-activedescendant`, focus traps.
- **co-25 · controlled-vs-uncontrolled** — a controlled input owns its `value`; an uncontrolled one reads via a ref; never switch mid-life.
- **co-26 · accessible-forms** — `required`/`aria-required`, `aria-invalid`, `aria-describedby`, and error summaries.
- **co-27 · performance-budget** — enforce limits on bundle size, request count, and timing.
- **co-28 · http-caching-swr** — `Cache-Control` with `stale-while-revalidate` (RFC 5861) and service-worker caching.
- **co-29 · css-at-scale** — CSS Modules (locally scoped) vs utility-first (Tailwind); the cascade/specificity ID–CLASS–TYPE model.
- **co-30 · data-fetching-patterns** — avoid request waterfalls via prefetching; Suspense-for-data triggers on `use()`, not `useEffect`.
- **co-31 · image-optimization** — responsive `srcset`/`sizes`, `next/image` lazy-by-default, reserved dimensions.
- **co-32 · edge-rendering** — render at the CDN edge in a V8-isolate Web-API subset runtime.
- **co-33 · typescript-frontend** — discriminated unions for state, generics in components, precise prop typing.
- **co-34 · pwa-service-worker** — offline caching, the app-shell model, and installability via a manifest.
- **co-35 · i18n** — locale-aware `Intl` formatting, message catalogs, and RTL layout.
- **co-36 · testing-behaviour** — test the way software is used (Testing Library) plus a Playwright e2e smoke.
- **co-37 · error-boundary** — catch a render error in a subtree and show a fallback instead of crashing the app.

## Worked examples

Colocated under `advanced-frontend/learning/code/` as typed TypeScript; each runnable + measured from the
CLI (DD-20/DD-30/DD-34). Contiguous `ex-01..ex-80`. Every example cites the `co-NN` it exercises; concepts
are taught before the examples that use them.

### Beginner

- **ex-01 · csr-render** — a client-rendered component with an empty initial HTML shell — verify the shell has no content pre-JS. (co-01)
- **ex-02 · ssr-render** — render a component to an HTML string on the server — verify the markup is complete pre-JS. (co-02)
- **ex-03 · ssg-build** — pre-render a page at build time — verify the built HTML is static. (co-03)
- **ex-04 · streaming-ssr-suspense** — stream HTML with a `<Suspense>` fallback — verify the fallback streams before the resolved content. (co-04)
- **ex-05 · hydration-attach** — hydrate server HTML with `hydrateRoot` — verify interactivity works after hydration. (co-05)
- **ex-06 · hydration-mismatch** — a server/client render mismatch — verify the mismatch warning appears. (co-05)
- **ex-07 · islands-astro** — a static page with one interactive island — verify only the island ships JS. (co-06)
- **ex-08 · use-client-boundary** — mark a client component with `'use client'` — verify it becomes interactive. (co-07)
- **ex-09 · server-component-default** — a server component renders with no client JS — verify no bundle is shipped for it. (co-07)
- **ex-10 · lcp-measure** — measure LCP and identify the LCP element — verify the reported element. (co-08, co-09)
- **ex-11 · inp-measure** — measure INP on an interaction — verify the reported latency. (co-08, co-10)
- **ex-12 · cls-fix** — reserve space to remove a layout shift — verify CLS drops toward 0. (co-08, co-11)
- **ex-13 · cwv-thresholds-table** — annotate LCP/INP/CLS good/poor thresholds — verify each against web.dev. (co-08)
- **ex-14 · render-blocking-css** — a blocking stylesheet delays first render — verify the delayed paint. (co-12)
- **ex-15 · critical-css-inline** — inline critical CSS above the fold — verify faster first paint. (co-12)
- **ex-16 · code-split-dynamic** — `React.lazy` + `<Suspense>` split a component — verify a separate chunk loads on demand. (co-13)
- **ex-17 · route-based-splitting** — split bundles per route — verify each route loads its own chunk. (co-13)
- **ex-18 · tree-shaking** — an unused export is dropped from the bundle — verify the dead code is absent. (co-14)
- **ex-19 · rules-of-hooks-violation** — a conditional hook fails the lint — verify the rule flags it. (co-17)
- **ex-20 · usestate-basic** — a state hook re-renders on change — verify the UI updates. (co-17)
- **ex-21 · useeffect-after-paint** — an effect runs after the browser paints — verify ordering. (co-18)
- **ex-22 · useeffect-cleanup** — cleanup runs before the next run/unmount — verify the cleanup fires. (co-18)
- **ex-23 · useeffect-deps** — the dependency array gates re-runs — verify a stable dep skips the effect. (co-18)
- **ex-24 · usememo-cache** — cache an expensive calculation — verify it recomputes only on dep change. (co-19)
- **ex-25 · usecallback-stable** — a stable function reference across renders — verify referential equality. (co-19)
- **ex-26 · keys-list** — keys give list items stable identity — verify state stays with the right item on reorder. (co-20)
- **ex-27 · keys-unstable-bug** — random keys recreate DOM nodes — verify the lost input state. (co-20)
- **ex-28 · controlled-input** — a controlled input with `value` + `onChange` — verify React owns the value. (co-25)

### Intermediate

- **ex-29 · uncontrolled-input** — an uncontrolled input read via a ref — verify the DOM owns the value. (co-25)
- **ex-30 · controlled-vs-uncontrolled** — a component cannot switch modes mid-life — verify the warning. (co-25)
- **ex-31 · server-cache-state** — TanStack Query caches server state — verify a cached read skips the network. (co-15)
- **ex-32 · client-state-separate** — client UI state kept separate from the server cache — verify they update independently. (co-15)
- **ex-33 · query-invalidation** — invalidate and refetch on a mutation — verify the stale query refetches. (co-15)
- **ex-34 · optimistic-update** — an optimistic UI that rolls back on failure — verify rollback on a simulated error. (co-15)
- **ex-35 · when-redux** — global shared state justifies a Redux Toolkit store — verify a slice + selector. (co-16)
- **ex-36 · derived-selector** — a memoized derived selector — verify it recomputes only on input change. (co-19)
- **ex-37 · virtual-dom-diff** — diff two virtual trees and patch the delta — verify only changed nodes update. (co-21)
- **ex-38 · reconciliation-same-type** — a same-type element reuses its DOM node — verify the node is preserved. (co-20, co-21)
- **ex-39 · reconciliation-different-type** — a different-type element rebuilds the subtree — verify the teardown. (co-21)
- **ex-40 · signal-basic** — a `.value` signal re-renders its readers on change — verify the update. (co-22)
- **ex-41 · signal-computed** — a computed signal tracks its dependencies — verify it recomputes on dep change. (co-22)
- **ex-42 · signal-vs-rerender** — a signal updates the DOM without a full component re-render — verify no parent re-render. (co-22)
- **ex-43 · semantic-html** — a `nav`/`main`/`article` document outline — verify the landmark structure. (co-23)
- **ex-44 · aria-widget** — an ARIA tabs widget with roles/states — verify the correct roles. (co-23)
- **ex-45 · focus-management-roving** — roving tabindex in a menu — verify arrow-key focus movement. (co-24)
- **ex-46 · focus-trap-modal** — trap focus inside a dialog — verify focus cannot escape. (co-24)
- **ex-47 · live-region** — an `aria-live` status announcement — verify the region announces updates. (co-23)
- **ex-48 · accessible-form-validation** — `required` + `aria-invalid` + `aria-describedby` — verify the error is programmatically associated. (co-26)
- **ex-49 · form-error-summary** — an accessible error summary that moves focus — verify focus lands on the summary. (co-26)
- **ex-50 · css-modules-scope** — locally scoped class names — verify no global collision. (co-29)
- **ex-51 · tailwind-utility** — utility-first styling of a component — verify the rendered styles. (co-29)
- **ex-52 · specificity-conflict** — resolve a cascade/specificity conflict — verify the winning rule. (co-29)
- **ex-53 · http-cache-headers** — `Cache-Control: max-age=…, stale-while-revalidate=…` — verify a stale response serves while revalidating. (co-28)
- **ex-54 · service-worker-cache** — a service worker caches assets for offline — verify an offline load succeeds. (co-28, co-34)
- **ex-55 · performance-budget-check** — a bundle-size budget fails a CI check on regression — verify the failing threshold. (co-27)
- **ex-56 · bundle-analysis** — analyze the bundle for an oversized dependency — verify the dependency is identified. (co-27)

### Advanced

- **ex-57 · data-waterfall** — a serial fetch waterfall — verify the sequential timing. (co-30)
- **ex-58 · prefetch-parallel** — parallelize with prefetching — verify the reduced total latency. (co-30)
- **ex-59 · suspense-for-data** — read a promise via `use()` under `<Suspense>` — verify the fallback then the data. (co-30)
- **ex-60 · suspense-not-useeffect** — Suspense does not detect a `useEffect` fetch — verify the fallback never shows. (co-30)
- **ex-61 · image-srcset** — responsive `srcset` + `sizes` — verify the right source is chosen per viewport. (co-31)
- **ex-62 · image-lazy-default** — `next/image` lazy-loads by default — verify off-screen images defer. (co-31)
- **ex-63 · image-cls-prevent** — width/height reserve space to prevent shift — verify CLS stays low. (co-31, co-11)
- **ex-64 · edge-runtime** — an edge function renders close to the user — verify it runs in the edge runtime. (co-32)
- **ex-65 · edge-vs-node** — the edge runtime's Web-API subset — verify a Node-only API is unavailable. (co-32)
- **ex-66 · discriminated-union-state** — model loading/error/success as a discriminated union — verify exhaustive handling. (co-33)
- **ex-67 · exhaustive-switch** — exhaustive narrowing over the union — verify a missing case is a type error. (co-33)
- **ex-68 · generic-component** — a typed generic list component — verify type inference on items. (co-33)
- **ex-69 · prop-typing** — precise required/optional prop typing — verify a missing required prop is a type error. (co-33)
- **ex-70 · pwa-app-shell** — an app-shell cached by a service worker — verify the shell loads instantly on repeat visits. (co-34)
- **ex-71 · pwa-installable** — a web manifest makes the app installable — verify the install prompt criteria. (co-34)
- **ex-72 · offline-fallback** — an offline fallback page — verify it serves when the network is down. (co-34)
- **ex-73 · i18n-format** — locale-aware number/date formatting via `Intl` — verify per-locale output. (co-35)
- **ex-74 · i18n-messages** — a message catalog with interpolation — verify the interpolated translation. (co-35)
- **ex-75 · i18n-rtl** — an RTL-aware layout — verify the mirrored direction. (co-35)
- **ex-76 · rtl-testing-behaviour** — a Testing-Library test querying by role/text — verify it tests behaviour, not internals. (co-36)
- **ex-77 · rtl-interaction** — a `user-event` interaction test — verify the user flow. (co-36)
- **ex-78 · playwright-e2e** — a Playwright e2e smoke test — verify the critical path in a real browser. (co-36)
- **ex-79 · error-boundary** — an error boundary catches a render error — verify the fallback renders instead of a crash. (co-37)
- **ex-80 · advanced-frontend-capstone** — a searchable, paginated dashboard: SSR/streaming + cached fetch + optimistic update + derived state + an ARIA-correct widget + component and e2e tests — verify measured Core Web Vitals improve, the optimistic update rolls back, the widget is keyboard-operable, and all tests pass. (co-04, co-08, co-15, co-24, co-30, co-36)

## Capstone spec — intra-topic (subject → full runnable)

- **Goal**: build a performant, accessible data-driven feature (e.g. a searchable, paginated dashboard
  panel) with SSR/streaming, cached data fetching + optimistic updates, derived state, an ARIA-correct
  custom widget, and error boundaries — measured with Lighthouse and covered by component + e2e tests.
- **Concepts exercised**: [ ] a measured perf fix — Core Web Vitals (co-08, co-27) [ ] SSR/streaming +
  hydration (co-04, co-05) [ ] cached fetch + optimistic update (co-15) [ ] derived/shared state (co-19)
  [ ] an ARIA-correct widget + focus/live region (co-23, co-24) [ ] Testing-Library + a Playwright e2e
  smoke (co-36).
- **Ordered steps**:
  1. `.../learning/capstone/code/` — the feature with SSR + a slow baseline. Verify it renders server-side
     and record a baseline Lighthouse score.
  2. Apply perf work (code-split, memoize, lazy-load). Verify Lighthouse LCP/INP improve measurably vs
     baseline.
  3. Add cached data fetching + an optimistic update + derived selectors. Verify the optimistic update
     rolls back on a simulated failure.
  4. Add an ARIA-correct custom widget with focus management + a live region; write a Testing-Library
     interaction test and a Playwright e2e smoke. Verify both pass and the widget is keyboard-operable.
- **Acceptance criteria**: measured Core Web Vitals improve over baseline; optimistic update + rollback
  works; the widget is ARIA-correct and keyboard-operable; component + e2e tests green.
- **Done bar**: runnable end-to-end + web-verified.

## Read more

**Books**

- **High Performance Browser Networking** — Ilya Grigorik (2013). Free, author-hosted classic on the networking layer (TCP, TLS, HTTP/2) that underlies frontend performance work. <https://hpbn.co/>
- **Micro Frontends in Action** — Michael Geers (2020). Standard reference for scaling frontend architecture and rendering ownership across teams.

**Papers & articles**

- **Rendering on the Web** — Google Chrome / web.dev team (ongoing). Canonical, framework-agnostic explanation of CSR/SSR/SSG/ISR rendering patterns and their tradeoffs. <https://web.dev/articles/rendering-on-the-web>
- **React Documentation** — Meta / React core team (ongoing). The authoritative source on modern React internals (reconciliation, concurrent rendering, hooks) that much of "advanced frontend" curricula builds on. <https://react.dev/learn>
- **The Cost of JavaScript in 2019** — Addy Osmani (2019), V8 team blog. Widely cited, data-driven reframing of frontend performance around JavaScript parse/compile/execute cost, not just download size. <https://v8.dev/blog/cost-of-javascript-2019>

---

← Previous: [46 · Distributed Systems](./46-distributed-systems.md) · Next: [48 · Build Your Own Reactive UI](./48-build-your-own-reactive-ui.md) →
