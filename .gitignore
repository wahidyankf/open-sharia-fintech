# Dependencies
node_modules/
yarn.lock
pnpm-lock.yaml

# Build outputs
dist/
build/
*.tsbuildinfo
.next/
apps/*/out/
cover.out
coverage.html
/static/
storybook-static/

# Nx cache
.nx/

# Homebrew
Brewfile.lock.json

# Environment variables
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
.env.test
.env.prod
.env.stag
!.env.example

# IDE and editor
# .vscode/
.idea/
.settings/
.classpath
.project
.factorypath
*.swp
*.swo
*~
.DS_Store
*.iml
.gradle/

# Excalidraw files in docs
docs/**/*.excalidraw.md

# Testing
coverage/
!apps/rhino-cli/internal/coverage/
!apps/rhino-cli/tests/unit/Fixtures/TestContract/Coverage/
coverage.json
.nyc_output/
jest.config.js.bak

# Playwright output
playwright-report/
test-results/
apps/ayokoding-www-fe-e2e/.e2e-artifacts/

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS files
Thumbs.db
.DS_Store

# Temporary files
tmp/
*.tmp
local-tmp/*
!local-tmp/.gitkeep

# Generated reports
generated-reports/

# Cache
.eslintcache
.cache/

# Link validation cache (note: this file is committed to share verification state)

# Playwright MCP
.playwright-mcp/

# Database
*.db
*.sqlite
*.sqlite3

# API keys and secrets
.secrets/
secrets.json

# Sensitive fintech files
*.pem
*.key
*.crt
*.pfx

# Java / Maven / Gradle build artifacts
target/
*.class
*.jar
*.exec

# Rust build artifacts + shared-cache `target` symlinks created by
# `rhino-cli doctor --fix` (the `target/` rule above matches directories only,
# not the symlink form, so the symlinked `target` entries need their own rule)
target

# Python cache and virtual environments
__pycache__/
*.pyc
*.pyo
*.egg-info/
.ruff_cache/
.pytest_cache/
.mypy_cache/
.venv/
.coverage

# Go intermediate coverage files and build artifacts
cover_*.out
apps/a-demo-be-golang-gin/server

# .NET / F# build artifacts
[Bb]in/
[Oo]bj/
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
*.binlog
# AltCover default output
coverage.xml

# Dart tool state (generated locally by pub and test commands)
**/.dart_tool/

# File uploads (generated by integration tests, e.g. demo-be-java-vertx attachments)
**/file-uploads/

# Generated contract code (regenerated by nx run <app>:codegen)
**/generated-contracts/
# Python variant (Python packages use underscores, not hyphens)
**/generated_contracts/
# Bundled OpenAPI spec + docs
specs/apps/a-demo/contracts/generated/

# MemSearch vector index (local user-specific data)
.memsearch/

# Claude Code settings
.claude/settings.local.json
.claude/settings.local.json.bkup
.claude/worktrees/
/worktrees/*
!/worktrees/.gitkeep

# Manual-verification evidence belongs in the owning plan's evidence/ subfolder
# (plans/{backlog,in-progress,done}/<slug>/evidence/), which moves with the plan on
# archival — see repo-governance/development/quality/evidence-capture/.
# A repo-root evidence/ is always a misplacement. The leading slash anchors this to
# the root, so per-plan evidence/ folders are unaffected.
/evidence/

# OpenCode generated files (package.json and lockfiles are local MCP runner artifacts)
.opencode/package-lock.json
.opencode/settings.local.json
.opencode/opencode.local.json
.claude/*.lock

# crane-cli integration test execution-chain state files (created in working dir during test runs)
.execution-chain-*

# Downloaded CI tooling binaries (downloaded on-demand during local testing)
actionlint

# Vercel CLI link state — contains orgId/projectId, must not land in this public repo
.vercel/

# FVM Version Cache
.fvm/
.fvm-cache/
