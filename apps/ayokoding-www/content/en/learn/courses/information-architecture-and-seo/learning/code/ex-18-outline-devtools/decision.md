# Inspect the document outline

Open browser developer tools, select the accessibility tree, and confirm:

1. a single page-topic heading is exposed;
2. navigation landmarks have distinct labels;
3. headings descend without skipped levels.

Use an automated report as a prompt for review, then confirm the page’s intended structure yourself.
