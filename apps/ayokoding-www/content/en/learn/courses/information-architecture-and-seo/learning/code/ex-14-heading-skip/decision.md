# Correct a skipped level

Bad: `h1 → h3`

Good: `h1 → h2 → h3`

Choose headings for the document structure, rather than selecting a level for visual size. CSS controls size; headings expose the hierarchy to assistive technology and search engines.
