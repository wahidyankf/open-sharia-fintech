# Hyphenated URL

```text
/guides/configure-payment-reminders
```

Hyphens separate readable words. Avoid spaces, opaque identifiers, and underscores in public content paths unless an external compatibility contract requires them.
