---
title: "Scheme Foundations"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 10
---

## Example 1: s-expr-eval

_ex-01_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 2: atom-vs-list

_ex-02_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 3: cons-pair

_ex-03_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 4: car-cdr

_ex-04_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 5: list-construct

_ex-05_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 6: quote-list

_ex-06_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 7: define-var

_ex-07_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 8: define-fn

_ex-08_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 9: lambda-basic

_ex-09_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 10: closure-capture

_ex-10_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 11: if-cond

_ex-11_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 12: let-binding

_ex-12_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 13: let-star

_ex-13_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 14: recursion-factorial

_ex-14_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 15: recursion-list-length

_ex-15_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 16: recursion-list-sum

_ex-16_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 17: map-example

_ex-17_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 18: filter-example

_ex-18_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 19: fold-example

_ex-19_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 20: repl-workflow

_ex-20_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 21: vector-basic

_ex-21_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 22: clojure-hello

_ex-22_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 23: clojure-list-vector

_ex-23_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 24: clojure-map

_ex-24_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 25: clojure-first-rest

_ex-25_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 26: clojure-recursion

_ex-26_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.
