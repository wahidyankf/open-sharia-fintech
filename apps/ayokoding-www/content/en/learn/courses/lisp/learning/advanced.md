---
title: "Metaprogramming"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 30
---

## Example 53: syntax-rules-binding-form

_ex-53_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 54: macro-recursive-expansion

_ex-54_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 55: macro-with-guard

_ex-55_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 56: hygiene-vs-capture

_ex-56_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 57: continuation-callcc

_ex-57_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 58: callcc-early-exit

_ex-58_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 59: callcc-generator

_ex-59_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 60: mini-interpreter

_ex-60_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 61: mini-interpreter-env

_ex-61_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 62: dsl-via-macros

_ex-62_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 63: fold-based-eval

_ex-63_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 64: recursion-tree-walk

_ex-64_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 65: clojure-structural-sharing

_ex-65_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 66: clojure-seq-lazy

_ex-66_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 67: clojure-threading-macro

_ex-67_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 68: clojure-dsl-macro

_ex-68_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 69: scheme-clojure-parity

_ex-69_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 70: lisp-family-contrast

_ex-70_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 71: repl-driven-macro-dev

_ex-71_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 72: quote-eval-roundtrip

_ex-72_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 73: hygienic-macro-verified

_ex-73_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 74: macro-adds-control-form

_ex-74_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 75: both-lisps-run

_ex-75_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 76: full-lisp-slice

_ex-76_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 77: integration-scheme-clojure

_ex-77_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 78: capstone-lisp-macro

_ex-78_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.
