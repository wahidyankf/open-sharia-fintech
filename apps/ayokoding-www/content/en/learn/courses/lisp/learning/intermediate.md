---
title: "Macros and Clojure"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 20
---

## Example 27: tail-recursion

_ex-27_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 28: non-tail-vs-tail

_ex-28_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 29: hof-compose

_ex-29_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 30: hof-return-fn

_ex-30_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 31: named-let

_ex-31_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 32: assoc-list

_ex-32_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 33: quasiquote-template

_ex-33_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 34: unquote-splicing

_ex-34_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 35: eval-data-as-code

_ex-35_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 36: build-expr-then-eval

_ex-36_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 37: homoiconicity-inspect

_ex-37_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 38: repl-define-redefine

_ex-38_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 39: syntax-rules-swap

_ex-39_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 40: syntax-rules-unless

_ex-40_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 41: macro-pattern-ellipsis

_ex-41_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 42: macro-hygiene-demo

_ex-42_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 43: macroexpand-inspect

_ex-43_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 44: clojure-hof

_ex-44_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 45: clojure-immutable

_ex-45_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 46: clojure-set

_ex-46_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 47: clojure-code-as-data

_ex-47_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 48: clojure-defmacro-basic

_ex-48_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 49: clojure-macroexpand

_ex-49_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 50: clojure-gensym

_ex-50_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 51: clojure-java-interop

_ex-51_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.

## Example 52: scheme-vs-clojure-macro

_ex-52_

This original Scheme-first exercise isolates the named language decision. Its colocated artifact
is runnable with Racket where the installed toolchain is available; Clojure-labelled exercises are
sidebars that preserve the same code-as-data idea on the JVM.
