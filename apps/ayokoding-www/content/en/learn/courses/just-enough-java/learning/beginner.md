---
title: "Beginner Examples"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 10
---

The first 26 examples establish terminal feedback, values, objects, and basic collections. Build
each standalone source with javac where its folder README says it is supported.

## Example 1: maven-project

_ex-01 · exercises co-01_

The colocated source at code/ex-01-maven-project/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 2: hello-main

_ex-02 · exercises co-02_

The colocated source at code/ex-02-hello-main/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 3: run-build-tool

_ex-03 · exercises co-01_

The colocated source at code/ex-03-run-build-tool/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 4: primitives

_ex-04 · exercises co-03_

The colocated source at code/ex-04-primitives/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 5: boolean-char

_ex-05 · exercises co-03_

The colocated source at code/ex-05-boolean-char/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 6: wrapper-boxing

_ex-06 · exercises co-03_

The colocated source at code/ex-06-wrapper-boxing/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 7: class-def

_ex-07 · exercises co-04_

The colocated source at code/ex-07-class-def/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 8: class-methods

_ex-08 · exercises co-04_

The colocated source at code/ex-08-class-methods/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 9: constructor

_ex-09 · exercises co-04_

The colocated source at code/ex-09-constructor/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 10: interface

_ex-10 · exercises co-05_

The colocated source at code/ex-10-interface/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 11: inheritance

_ex-11 · exercises co-06_

The colocated source at code/ex-11-inheritance/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 12: record-basic

_ex-12 · exercises co-07_

The colocated source at code/ex-12-record-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 13: record-equals

_ex-13 · exercises co-07_

The colocated source at code/ex-13-record-equals/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 14: enum-basic

_ex-14 · exercises co-09_

The colocated source at code/ex-14-enum-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 15: enum-switch

_ex-15 · exercises co-09, co-11_

The colocated source at code/ex-15-enum-switch/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 16: list-basic

_ex-16 · exercises co-15_

The colocated source at code/ex-16-list-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 17: list-iterate

_ex-17 · exercises co-15_

The colocated source at code/ex-17-list-iterate/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 18: map-basic

_ex-18 · exercises co-16_

The colocated source at code/ex-18-map-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 19: set-basic

_ex-19 · exercises co-17_

The colocated source at code/ex-19-set-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 20: generic-list

_ex-20 · exercises co-13, co-15_

The colocated source at code/ex-20-generic-list/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 21: for-each

_ex-21 · exercises co-15_

The colocated source at code/ex-21-for-each/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 22: string-methods

_ex-22 · exercises co-03_

The colocated source at code/ex-22-string-methods/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 23: try-catch

_ex-23 · exercises co-24_

The colocated source at code/ex-23-try-catch/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 24: checked-exception

_ex-24 · exercises co-24_

The colocated source at code/ex-24-checked-exception/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 25: null-handling

_ex-25 · exercises co-24_

The colocated source at code/ex-25-null-handling/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 26: equals-vs-identity

_ex-26 · exercises co-25_

The colocated source at code/ex-26-equals-vs-identity/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.
