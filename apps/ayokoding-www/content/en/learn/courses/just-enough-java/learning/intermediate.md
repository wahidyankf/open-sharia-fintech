---
title: "Intermediate Examples"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 20
---

Examples 27–52 introduce modern data modeling, pattern matching, generics, streams, and deliberate
absence. The Java 25 guard and compact-source examples are documented as version-specific.

## Example 27: record-compact-ctor

_ex-27 · exercises co-07_

The colocated source at code/ex-27-record-compact-ctor/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 28: sealed-interface

_ex-28 · exercises co-08_

The colocated source at code/ex-28-sealed-interface/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 29: sealed-record-hierarchy

_ex-29 · exercises co-08, co-07_

The colocated source at code/ex-29-sealed-record-hierarchy/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 30: instanceof-pattern

_ex-30 · exercises co-10_

The colocated source at code/ex-30-instanceof-pattern/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 31: switch-pattern

_ex-31 · exercises co-11_

The colocated source at code/ex-31-switch-pattern/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 32: switch-exhaustive

_ex-32 · exercises co-12_

The colocated source at code/ex-32-switch-exhaustive/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 33: switch-guard

_ex-33 · exercises co-11_

The colocated source at code/ex-33-switch-guard/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 34: switch-record-deconstruct

_ex-34 · exercises co-11, co-07_

The colocated source at code/ex-34-switch-record-deconstruct/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 35: generic-method

_ex-35 · exercises co-13_

The colocated source at code/ex-35-generic-method/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 36: generic-class

_ex-36 · exercises co-13_

The colocated source at code/ex-36-generic-class/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 37: bounded-type

_ex-37 · exercises co-14_

The colocated source at code/ex-37-bounded-type/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 38: wildcard

_ex-38 · exercises co-14_

The colocated source at code/ex-38-wildcard/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 39: map-iterate

_ex-39 · exercises co-16_

The colocated source at code/ex-39-map-iterate/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 40: map-compute

_ex-40 · exercises co-16_

The colocated source at code/ex-40-map-compute/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 41: stream-map

_ex-41 · exercises co-18_

The colocated source at code/ex-41-stream-map/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 42: stream-filter

_ex-42 · exercises co-18_

The colocated source at code/ex-42-stream-filter/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 43: stream-collect-list

_ex-43 · exercises co-19_

The colocated source at code/ex-43-stream-collect-list/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 44: stream-collect-map

_ex-44 · exercises co-19_

The colocated source at code/ex-44-stream-collect-map/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 45: stream-reduce

_ex-45 · exercises co-20_

The colocated source at code/ex-45-stream-reduce/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 46: stream-count

_ex-46 · exercises co-20_

The colocated source at code/ex-46-stream-count/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 47: lambda-basic

_ex-47 · exercises co-21_

The colocated source at code/ex-47-lambda-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 48: lambda-comparator

_ex-48 · exercises co-21_

The colocated source at code/ex-48-lambda-comparator/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 49: method-reference

_ex-49 · exercises co-22_

The colocated source at code/ex-49-method-reference/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 50: optional-basic

_ex-50 · exercises co-23_

The colocated source at code/ex-50-optional-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 51: optional-map

_ex-51 · exercises co-23_

The colocated source at code/ex-51-optional-map/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 52: stream-of-records

_ex-52 · exercises co-18, co-07_

The colocated source at code/ex-52-stream-of-records/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.
