---
title: "Advanced Examples"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 30
---

Examples 53–80 combine the primer's small parts, then close with test/build and single-file launch
modes. These are exercises in readable application code, not an enterprise framework curriculum.

## Example 53: grouping-collector

_ex-53 · exercises co-19_

The colocated source at code/ex-53-grouping-collector/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 54: stream-flatmap

_ex-54 · exercises co-18_

The colocated source at code/ex-54-stream-flatmap/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 55: stream-sorted

_ex-55 · exercises co-18_

The colocated source at code/ex-55-stream-sorted/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 56: stream-distinct

_ex-56 · exercises co-18_

The colocated source at code/ex-56-stream-distinct/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 57: sealed-visitor

_ex-57 · exercises co-08, co-11_

The colocated source at code/ex-57-sealed-visitor/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 58: nested-record-pattern

_ex-58 · exercises co-11_

The colocated source at code/ex-58-nested-record-pattern/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 59: generic-bounded-method

_ex-59 · exercises co-14, co-18_

The colocated source at code/ex-59-generic-bounded-method/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 60: optional-in-stream

_ex-60 · exercises co-23, co-18_

The colocated source at code/ex-60-optional-in-stream/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 61: exception-in-stream

_ex-61 · exercises co-24, co-18_

The colocated source at code/ex-61-exception-in-stream/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 62: custom-exception

_ex-62 · exercises co-24_

The colocated source at code/ex-62-custom-exception/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 63: immutable-record-collection

_ex-63 · exercises co-07, co-15_

The colocated source at code/ex-63-immutable-record-collection/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 64: record-as-map-key

_ex-64 · exercises co-07, co-16_

The colocated source at code/ex-64-record-as-map-key/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 65: enum-with-fields

_ex-65 · exercises co-09_

The colocated source at code/ex-65-enum-with-fields/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 66: interface-default-method

_ex-66 · exercises co-05_

The colocated source at code/ex-66-interface-default-method/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 67: gc-object-lifecycle

_ex-67 · exercises co-25_

The colocated source at code/ex-67-gc-object-lifecycle/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 68: heap-vs-stack

_ex-68 · exercises co-25_

The colocated source at code/ex-68-heap-vs-stack/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 69: junit-test-basic

_ex-69 · exercises co-26_

The colocated source at code/ex-69-junit-test-basic/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 70: junit-assertions

_ex-70 · exercises co-26_

The colocated source at code/ex-70-junit-assertions/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 71: junit-parameterized

_ex-71 · exercises co-26_

The colocated source at code/ex-71-junit-parameterized/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 72: build-run-test

_ex-72 · exercises co-01, co-26_

The colocated source at code/ex-72-build-run-test/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 73: streams-pipeline-full

_ex-73 · exercises co-18, co-19, co-20_

The colocated source at code/ex-73-streams-pipeline-full/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 74: sealed-plus-pattern-full

_ex-74 · exercises co-08, co-12_

The colocated source at code/ex-74-sealed-plus-pattern-full/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 75: generics-collections-streams

_ex-75 · exercises co-13, co-15, co-18_

The colocated source at code/ex-75-generics-collections-streams/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 76: full-primer-slice

_ex-76 · exercises co-07, co-08, co-11, co-18_

The colocated source at code/ex-76-full-primer-slice/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 77: integration-build-test

_ex-77 · exercises co-01, co-26_

The colocated source at code/ex-77-integration-build-test/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 78: capstone-java-primer

_ex-78 · exercises co-07, co-08, co-11, co-18, co-01, co-26_

The colocated source at code/ex-78-capstone-java-primer/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 79: single-file-source-run

_ex-79 · exercises co-27_

The colocated source at code/ex-79-single-file-source-run/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.

## Example 80: compact-source-instance-main

_ex-80 · exercises co-28, co-02_

The colocated source at code/ex-80-compact-source-instance-main/ is an original, small Java artifact. Read the
comments, compile it with a current JDK where applicable, and change one value before continuing.
The observable check is that the named construct makes the intended value or control-flow decision
visible; do not treat the printed label alone as proof of correctness.
