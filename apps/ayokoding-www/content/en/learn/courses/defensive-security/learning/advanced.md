---
title: "Advanced Examples"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 30
---

> **Safe lab boundary.** Every command reads this course's synthetic files only. Do not replace them with production logs, customer data, or a target you do not own and authorize.

## Worked examples

### Example 53: Segment a Training Network

**What you will do.** Make one observable, defensive decision for `co-27` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-27)

### Example 54: Review a Deny-by-Default Firewall

**What you will do.** Make one observable, defensive decision for `co-27` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-27)

### Example 55: Apply a CIS-Inspired Control

**What you will do.** Make one observable, defensive decision for `co-28` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-28)

### Example 56: Reduce an Attack Surface

**What you will do.** Make one observable, defensive decision for `co-28` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-28)

### Example 57: Inventory Lab Vulnerability Findings

**What you will do.** Make one observable, defensive decision for `co-29` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-29)

### Example 58: Run a Patch-Management Loop

**What you will do.** Make one observable, defensive decision for `co-29` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-29)

### Example 59: Prioritize High-Risk Remediation

**What you will do.** Make one observable, defensive decision for `co-29` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-29)

### Example 60: Design a Safe Decoy

**What you will do.** Make one observable, defensive decision for `co-30` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hardening
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-30)

### Example 61: Alert on Decoy Interaction

**What you will do.** Make one observable, defensive decision for `co-30` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py detect
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-30)

### Example 62: Model a SOAR Response Gate

**What you will do.** Make one observable, defensive decision for `co-34` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py ir
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-34)

### Example 63: Enrich an Alert Before Action

**What you will do.** Make one observable, defensive decision for `co-34` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py ir
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-34)

### Example 64: Recognize Alert Fatigue

**What you will do.** Make one observable, defensive decision for `co-33` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py detect
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-33)

### Example 65: Tune a Noisy Signal

**What you will do.** Make one observable, defensive decision for `co-33` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py detect
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-33)

### Example 66: Close a Purple-Team Loop

**What you will do.** Make one observable, defensive decision for `co-32` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py coverage
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-32)

### Example 67: Find a Coverage Gap

**What you will do.** Make one observable, defensive decision for `co-32` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py coverage
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-32)

### Example 68: Build a Detection Coverage Matrix

**What you will do.** Make one observable, defensive decision for `co-10, co-32` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py coverage
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-10, co-32)

### Example 69: Place an IDS in a Lab Path

**What you will do.** Make one observable, defensive decision for `co-13` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py detect
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-13)

### Example 70: Correlate Two Local Sources

**What you will do.** Make one observable, defensive decision for `co-04` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py detect
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-04)

### Example 71: Build an Attack Timeline View

**What you will do.** Make one observable, defensive decision for `co-05` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py timeline
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-05)

### Example 72: Gate Detection Changes in CI

**What you will do.** Make one observable, defensive decision for `co-07` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py verify
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-07)

### Example 73: Translate a Sigma Rule Deliberately

**What you will do.** Make one observable, defensive decision for `co-08` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py bulk
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-08)

### Example 74: Sweep Lab Events for an IOC

**What you will do.** Make one observable, defensive decision for `co-21` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hunt
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-21)

### Example 75: Promote a Hunt to a Detection

**What you will do.** Make one observable, defensive decision for `co-20, co-07` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py hunt
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-20, co-07)

### Example 76: Run an Incident Tabletop

**What you will do.** Make one observable, defensive decision for `co-15` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py tabletop
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-15)

### Example 77: Verify a Clean Restore

**What you will do.** Make one observable, defensive decision for `co-18` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py ir
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-18)

### Example 78: Complete the Blue-Team Capstone

**What you will do.** Make one observable, defensive decision for `co-04, co-08, co-10, co-15, co-32` against original synthetic telemetry; the fixture is never sent to a remote service.

```sh
# => Reads only bundled synthetic telemetry; this command opens no socket.
python3 code/blue_lab.py verify
```

**Key takeaway.** A defensive decision is useful only when another analyst can reproduce it from bounded evidence and explain its safety boundary.

**Why it matters.** Security operations improve when an analyst can repeat a decision from local, reviewable evidence. This exercise turns the concept into an observable check that supports review, tuning, and handoff. Because its inputs are synthetic and self-owned, repeating it cannot affect a production system or disclose another person's telemetry while still rehearsing professional practice. (co-04, co-08, co-10, co-15, co-32)
