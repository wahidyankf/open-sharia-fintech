## Runnable worked-example artifacts

Every code-bearing worked example links to its own source artifact here. Run Terraform/OpenTofu
examples that target AWS resources only while LocalStack is running on `localhost:4566`; generated
state and provider directories stay untracked. Artifacts that model a policy, state shape, or a
remote-backend decision remain complete, parseable examples but intentionally do not contact a paid
cloud endpoint.
