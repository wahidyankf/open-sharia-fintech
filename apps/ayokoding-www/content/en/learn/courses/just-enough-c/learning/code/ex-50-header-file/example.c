// => this directive makes a declaration available
#include <stdio.h>
// => this directive makes a declaration available
#include "answer.h"
// => this line is part of the complete runnable program
int main(void) {
  // => this line is part of the complete runnable program
  printf("answer=%d\n", answer());
  // => this line is part of the complete runnable program
  return 0;
  // => this line is part of the complete runnable program
}
