// => this directive makes a declaration available
#ifndef CONFIG_H
// => this directive makes a declaration available
#define CONFIG_H
// => this directive makes a declaration available
#define MESSAGE "guard works"
// => this directive makes a declaration available
#endif
