-- BUG: a bare URL string tracks whatever the default branch currently points to --
-- there is no explicit version field to inspect or reproduce later
vim.pack.add({ "https://github.com/sainnhe/gruvbox-material" })
