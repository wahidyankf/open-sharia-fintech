-- NOTE: .setup() is deliberately never called here either -- SAME as before/init.lua,
-- so the only variable between before/ and after/ is where the plugin module itself
-- registers the command
require("myplugin")
