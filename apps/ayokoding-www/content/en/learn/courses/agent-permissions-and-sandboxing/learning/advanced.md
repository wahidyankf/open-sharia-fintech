---
title: "Advanced Examples"
date: 2026-08-14T00:00:00+07:00
draft: false
weight: 12
---

### Example 35: Fully Sandboxed Coding Agent

**Brief explanation.** Fully Sandboxed Coding Agent composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-35-fully-sandboxed-coding-agent/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 36: Injection Resistant Web Agent

**Brief explanation.** Injection Resistant Web Agent composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-36-injection-resistant-web-agent/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 37: Egress Controlled Fleet

**Brief explanation.** Egress Controlled Fleet composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-37-egress-controlled-fleet/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 38: Escalation Audit

**Brief explanation.** Escalation Audit composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-38-escalation-audit/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 39: Defense In Depth

**Brief explanation.** Defense In Depth composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-39-defense-in-depth/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 40: Red Team The Agent

**Brief explanation.** Red Team The Agent composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-40-red-team-the-agent/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 41: Reversibility Under Failure

**Brief explanation.** Reversibility Under Failure composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-41-reversibility-under-failure/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 42: Policy As Code Review

**Brief explanation.** Policy As Code Review composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-42-policy-as-code-review/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 43: Sandbox Escape Awareness

**Brief explanation.** Sandbox Escape Awareness composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-43-sandbox-escape-awareness/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 44: Human Gate Workflow

**Brief explanation.** Human Gate Workflow composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-44-human-gate-workflow/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 45: Secret Scoped Tool

**Brief explanation.** Secret Scoped Tool composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-45-secret-scoped-tool/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 46: Least Privilege Refactor

**Brief explanation.** Least Privilege Refactor composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-46-least-privilege-refactor/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 47: Injection Guardrail Suite

**Brief explanation.** Injection Guardrail Suite composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-47-injection-guardrail-suite/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 48: Two Permission Profiles One Agent

**Brief explanation.** Two Permission Profiles One Agent composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-48-two-permission-profiles-one-agent/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 49: Exploration Profile Shipped To Production

**Brief explanation.** Exploration Profile Shipped To Production composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-49-exploration-profile-shipped-to-production/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 50: Risk Not Capability

**Brief explanation.** Risk Not Capability composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-50-risk-not-capability/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 51: Profile Promotion Checklist

**Brief explanation.** Profile Promotion Checklist composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-51-profile-promotion-checklist/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.

### Example 52: Capstone Guarded Agent

**Brief explanation.** Capstone Guarded Agent composes guardrails against a hostile case.

**Diagram.** `agent → policy → sandbox → audit`.

**Annotated code.** `learning/code/ex-52-capstone-guarded-agent/example.py`.

**Key takeaway.** Layers enforce safety outside model intent.

**Why it matters.** Production risk is constrained by blast radius, not claimed model ability.
