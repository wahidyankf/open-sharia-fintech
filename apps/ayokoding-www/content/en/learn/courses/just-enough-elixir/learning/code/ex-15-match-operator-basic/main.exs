# match operator basic: this expression exposes the Elixir value or match being learned.
x = 1
# match operator basic: this expression exposes the Elixir value or match being learned.
1 = x
# match operator basic: this expression exposes the Elixir value or match being learned.
IO.puts("matched #{x}")
