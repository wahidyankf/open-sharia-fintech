# prepend list: this expression exposes the Elixir value or match being learned.
list = [1, 2, 3]
# prepend list: this expression exposes the Elixir value or match being learned.
IO.inspect([0 | list])
