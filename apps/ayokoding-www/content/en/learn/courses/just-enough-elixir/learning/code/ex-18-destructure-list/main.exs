# destructure list: this expression exposes the Elixir value or match being learned.
[a, b, c] = [1, 2, 3]
# destructure list: this expression exposes the Elixir value or match being learned.
IO.inspect({a, b, c})
