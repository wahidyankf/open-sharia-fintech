# head tail match: this expression exposes the Elixir value or match being learned.
[head | tail] = [1, 2, 3]
# head tail match: this expression exposes the Elixir value or match being learned.
IO.inspect({head, tail})
