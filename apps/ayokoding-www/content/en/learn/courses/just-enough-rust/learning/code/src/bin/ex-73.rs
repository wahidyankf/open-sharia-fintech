// Cargo.toml records dependencies for cargo resolution.
// This primer needs no third-party crate to demonstrate it.
fn main() {
    println!("Add a dependency with cargo add <crate>; keep versions in Cargo.toml");
}
