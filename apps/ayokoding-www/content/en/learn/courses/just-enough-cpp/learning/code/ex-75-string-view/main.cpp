// => string-view: this line establishes the runnable C++ state or behavior.
#include <iostream>
// => string-view: this line establishes the runnable C++ state or behavior.
#include <string_view>
// => string-view: this line establishes the runnable C++ state or behavior.
void print(std::string_view text) {
// => string-view: this line establishes the runnable C++ state or behavior.
  std::cout << text << "\n";
// => string-view: this line establishes the runnable C++ state or behavior.
}
// => string-view: this line establishes the runnable C++ state or behavior.
int main() {
// => string-view: this line establishes the runnable C++ state or behavior.
  print("view");
// => string-view: this line establishes the runnable C++ state or behavior.
}
