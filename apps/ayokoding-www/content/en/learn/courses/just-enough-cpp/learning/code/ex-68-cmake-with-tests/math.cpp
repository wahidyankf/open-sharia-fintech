// => Imports the API declaration that this translation unit defines.
#include "math.hpp"
// => Implements deterministic arithmetic for the test target.
int add(int left, int right) { return left + right; }
