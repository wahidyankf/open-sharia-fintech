// => std-string: this line establishes the runnable C++ state or behavior.
#include <iostream>
// => std-string: this line establishes the runnable C++ state or behavior.
#include <string>
// => std-string: this line establishes the runnable C++ state or behavior.
int main() {
// => std-string: this line establishes the runnable C++ state or behavior.
  std::string name = "Ada";
// => std-string: this line establishes the runnable C++ state or behavior.
  name += " Lovelace";
// => std-string: this line establishes the runnable C++ state or behavior.
  std::cout << name.size() << "\n";
// => std-string: this line establishes the runnable C++ state or behavior.
}
