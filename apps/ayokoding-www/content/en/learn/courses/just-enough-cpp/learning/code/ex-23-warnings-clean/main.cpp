// => warnings-clean: this line establishes the runnable C++ state or behavior.
#include <iostream>
// => warnings-clean: this line establishes the runnable C++ state or behavior.
int main() {
// => warnings-clean: this line establishes the runnable C++ state or behavior.
  const int answer = 42;
// => warnings-clean: this line establishes the runnable C++ state or behavior.
  std::cout << answer << "\n";
// => warnings-clean: this line establishes the runnable C++ state or behavior.
}
