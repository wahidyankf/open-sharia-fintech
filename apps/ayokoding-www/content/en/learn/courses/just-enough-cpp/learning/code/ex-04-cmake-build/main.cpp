// => cmake-build: this line establishes the runnable C++ state or behavior.
#include <iostream>
// => cmake-build: this line establishes the runnable C++ state or behavior.
int main() {
// => cmake-build: this line establishes the runnable C++ state or behavior.
  std::cout << "cmake built this\n";
// => cmake-build: this line establishes the runnable C++ state or behavior.
}
