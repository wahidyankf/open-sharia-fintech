apiVersion: v1
kind: Secret
metadata:
  name: containers-capstone-secret
  namespace: containers-learning
type: Opaque
stringData:
  # => Placeholder only. Use an external secret manager or an untracked local mechanism for a real value.
  API_TOKEN: replace-me
