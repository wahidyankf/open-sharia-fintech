---
title: "Failures and KV"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 30
---

## Example 53: kv-state-machine

_ex-53_ · co-18, co-17

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-53-kv-state-machine/example.go`.

## Example 54: kv-put-get

_ex-54_ · co-18

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-54-kv-put-get/example.go`.

## Example 55: kv-linearizable-read

_ex-55_ · co-26

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-55-kv-linearizable-read/example.go`.

## Example 56: persist-term-vote

_ex-56_ · co-22

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-56-persist-term-vote/example.go`.

## Example 57: persist-log

_ex-57_ · co-22

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-57-persist-log/example.go`.

## Example 58: restart-recover-state

_ex-58_ · co-23, co-22

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-58-restart-recover-state/example.go`.

## Example 59: restart-no-double-vote

_ex-59_ · co-23, co-07

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-59-restart-no-double-vote/example.go`.

## Example 60: partition-minority-blocks

_ex-60_ · co-24

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-60-partition-minority-blocks/example.go`.

## Example 61: partition-majority-progresses

_ex-61_ · co-24, co-16

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-61-partition-majority-progresses/example.go`.

## Example 62: partition-heal-converge

_ex-62_ · co-24, co-13

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-62-partition-heal-converge/example.go`.

## Example 63: stale-leader-steps-down

_ex-63_ · co-03, co-02

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-63-stale-leader-steps-down/example.go`.

## Example 64: no-committed-loss

_ex-64_ · co-19, co-24

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-64-no-committed-loss/example.go`.

## Example 65: drop-messages-retry

_ex-65_ · co-25

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-65-drop-messages-retry/example.go`.

## Example 66: delayed-messages

_ex-66_ · co-25

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-66-delayed-messages/example.go`.

## Example 67: crash-restart-cycle

_ex-67_ · co-23

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-67-crash-restart-cycle/example.go`.

## Example 68: failure-harness

_ex-68_ · co-27

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-68-failure-harness/example.go`.

## Example 69: safety-under-failure

_ex-69_ · co-21, co-27

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-69-safety-under-failure/example.go`.

## Example 70: state-machine-safety-test

_ex-70_ · co-21

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-70-state-machine-safety-test/example.go`.

## Example 71: leader-completeness-test

_ex-71_ · co-19

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-71-leader-completeness-test/example.go`.

## Example 72: liveness-under-heal

_ex-72_ · co-28

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-72-liveness-under-heal/example.go`.

## Example 73: snapshot-stretch

_ex-73_ · co-29

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-73-snapshot-stretch/example.go`.

## Example 74: install-snapshot-stretch

_ex-74_ · co-29

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-74-install-snapshot-stretch/example.go`.

## Example 75: membership-change-stretch

_ex-75_ · co-30

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-75-membership-change-stretch/example.go`.

## Example 76: full-cluster-kv-cycle

_ex-76_ · co-18, co-24, co-26

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-76-full-cluster-kv-cycle/example.go`.

## Example 77: go-test-safety-liveness

_ex-77_ · co-27, co-28

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-77-go-test-safety-liveness/example.go`.

## Example 78: capstone-raft-kv

_ex-78_ · co-08, co-16, co-18, co-22, co-27

This isolated Go example makes one KV, persistence, partition, failure-injection, or liveness boundary observable. Source: `code/ex-78-capstone-raft-kv/example.go`.
