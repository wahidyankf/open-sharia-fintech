// Kata 3 after: The before version runs invalid work; the after version guards execution with availability.
// => Run this file and compare the bounded, observable result.
var canSave = false;
Console.WriteLine(canSave ? "saved" : "save blocked");
