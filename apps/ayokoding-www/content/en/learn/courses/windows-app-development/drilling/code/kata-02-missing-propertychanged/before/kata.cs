// Kata 2 before: The before version changes state silently; the after version raises a visible notification.
// => Run this file and identify why the behavior violates the UI contract.
var title = "changed without notification";
Console.WriteLine(title);
