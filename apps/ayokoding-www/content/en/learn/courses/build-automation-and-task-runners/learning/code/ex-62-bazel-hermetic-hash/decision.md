# External dependency integrity

An external archive declaration should include a source URL and a SHA-256 integrity value. The hash lets
the build tool reject bytes that do not match the declared external input.
