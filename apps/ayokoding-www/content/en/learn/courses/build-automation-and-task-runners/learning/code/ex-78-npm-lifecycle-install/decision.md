# npm installation lifecycle

Package installation can invoke declared lifecycle scripts such as prepare. These scripts are executable
package behavior and should be deterministic, auditable, and free of hidden external side effects.
