---
title: "Recovery and query"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 30
---

## Example 53: wal-append

_ex-53_ · co-14

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-53-wal-append/example.py`.

## Example 54: wal-record-format

_ex-54_ · co-14

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-54-wal-record-format/example.py`.

## Example 55: wal-before-page

_ex-55_ · co-16

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-55-wal-before-page/example.py`.

## Example 56: fsync-on-commit

_ex-56_ · co-15

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-56-fsync-on-commit/example.py`.

## Example 57: crash-mid-write-sim

_ex-57_ · co-17, co-20

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-57-crash-mid-write-sim/example.py`.

## Example 58: recovery-replay

_ex-58_ · co-17

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-58-recovery-replay/example.py`.

## Example 59: recovery-truncate

_ex-59_ · co-17

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-59-recovery-truncate/example.py`.

## Example 60: recovery-consistent-state

_ex-60_ · co-17

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-60-recovery-consistent-state/example.py`.

## Example 61: lose-uncommitted

_ex-61_ · co-17, co-27

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-61-lose-uncommitted/example.py`.

## Example 62: redo-recovery

_ex-62_ · co-18

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-62-redo-recovery/example.py`.

## Example 63: undo-recovery

_ex-63_ · co-18

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-63-undo-recovery/example.py`.

## Example 64: checkpoint-write

_ex-64_ · co-19

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-64-checkpoint-write/example.py`.

## Example 65: checkpoint-bounds-replay

_ex-65_ · co-19

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-65-checkpoint-bounds-replay/example.py`.

## Example 66: torn-write-detection

_ex-66_ · co-20

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-66-torn-write-detection/example.py`.

## Example 67: durability-latency-measure

_ex-67_ · co-21, co-15

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-67-durability-latency-measure/example.py`.

## Example 68: single-writer-txn

_ex-68_ · co-27

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-68-single-writer-txn/example.py`.

## Example 69: sql-tokenize

_ex-69_ · co-22

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-69-sql-tokenize/example.py`.

## Example 70: sql-parse-insert

_ex-70_ · co-22, co-23

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-70-sql-parse-insert/example.py`.

## Example 71: sql-parse-select

_ex-71_ · co-22, co-24

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-71-sql-parse-select/example.py`.

## Example 72: exec-insert

_ex-72_ · co-23, co-26

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-72-exec-insert/example.py`.

## Example 73: exec-select-all

_ex-73_ · co-24

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-73-exec-select-all/example.py`.

## Example 74: where-equality

_ex-74_ · co-25

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-74-where-equality/example.py`.

## Example 75: where-comparison

_ex-75_ · co-25

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-75-where-comparison/example.py`.

## Example 76: full-crash-cycle

_ex-76_ · co-17, co-24, co-27

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-76-full-crash-cycle/example.py`.

## Example 77: pytest-recovery

_ex-77_ · co-30, co-17

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-77-pytest-recovery/example.py`.

## Example 78: capstone-mini-db

_ex-78_ · co-02, co-06, co-14, co-17, co-24

This exercise makes one WAL, recovery, transaction, or SQL-ish boundary concrete. See the original isolated artifact in `code/ex-78-capstone-mini-db/example.py`.
