---
title: "Indexes and LSM alternatives"
date: 2026-08-15T00:00:00+07:00
draft: false
weight: 20
---

## Example 27: btree-leaf-node

_ex-27_ · co-06

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-27-btree-leaf-node/example.py`.

## Example 28: btree-internal-node

_ex-28_ · co-06

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-28-btree-internal-node/example.py`.

## Example 29: btree-insert-leaf

_ex-29_ · co-08

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-29-btree-insert-leaf/example.py`.

## Example 30: btree-search-key

_ex-30_ · co-07

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-30-btree-search-key/example.py`.

## Example 31: btree-search-missing

_ex-31_ · co-07

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-31-btree-search-missing/example.py`.

## Example 32: btree-ordered-scan

_ex-32_ · co-10

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-32-btree-ordered-scan/example.py`.

## Example 33: leaf-split

_ex-33_ · co-09

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-33-leaf-split/example.py`.

## Example 34: internal-split

_ex-34_ · co-09

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-34-internal-split/example.py`.

## Example 35: root-split-grow

_ex-35_ · co-09, co-06

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-35-root-split-grow/example.py`.

## Example 36: btree-many-inserts

_ex-36_ · co-08, co-10

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-36-btree-many-inserts/example.py`.

## Example 37: btree-over-pager

_ex-37_ · co-06, co-02

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-37-btree-over-pager/example.py`.

## Example 38: btree-delete

_ex-38_ · co-08

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-38-btree-delete/example.py`.

## Example 39: range-scan

_ex-39_ · co-10

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-39-range-scan/example.py`.

## Example 40: memtable-insert

_ex-40_ · co-11

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-40-memtable-insert/example.py`.

## Example 41: memtable-flush-sstable

_ex-41_ · co-11, co-12

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-41-memtable-flush-sstable/example.py`.

## Example 42: sstable-read

_ex-42_ · co-12

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-42-sstable-read/example.py`.

## Example 43: sstable-sorted

_ex-43_ · co-12

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-43-sstable-sorted/example.py`.

## Example 44: lsm-lookup-order

_ex-44_ · co-11

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-44-lsm-lookup-order/example.py`.

## Example 45: compaction-merge

_ex-45_ · co-13

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-45-compaction-merge/example.py`.

## Example 46: compaction-tombstone

_ex-46_ · co-13

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-46-compaction-tombstone/example.py`.

## Example 47: lsm-vs-btree-writeup

_ex-47_ · co-11, co-06

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-47-lsm-vs-btree-writeup/example.py`.

## Example 48: bloom-filter-taste

_ex-48_ · co-12

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-48-bloom-filter-taste/example.py`.

## Example 49: pytest-btree-splits

_ex-49_ · co-30, co-09

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-49-pytest-btree-splits/example.py`.

## Example 50: pytest-lsm-compaction

_ex-50_ · co-30, co-13

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-50-pytest-lsm-compaction/example.py`.

## Example 51: btree-height-log

_ex-51_ · co-07

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-51-btree-height-log/example.py`.

## Example 52: pager-btree-persist

_ex-52_ · co-06, co-02

This exercise makes one ordered-index or immutable-run trade-off concrete. See the original isolated artifact in `code/ex-52-pager-btree-persist/example.py`.
