//! Contract codegen post-processing: Dart scaffolding.
//!
//! Backs the `specs scaffold dart` command. Output formatting lives in the
//! command module itself (`crate::commands::specs_scaffold_dart`), matching
//! the rest of the `specs` command family.

pub mod dart_scaffold;
pub mod types;
