{
  "schema": "ose-test-contract-manifest-fixture/v1",
  "case": "a script that only forwards to the same project's Nx target",
  "project": "widget-e2e",
  "owner": "widget-app",
  "group": "dedicated-e2e",
  "disposition": "retained",
  "manifestPath": "apps/widget-e2e/package.json",
  "consumer": "Playwright resolves its config through the local package boundary",
  "requiredFields": ["name", "devDependencies"],
  "verification": "npx playwright test --list",
  "scripts": [{ "name": "test", "command": "npx nx run widget-e2e:test:e2e" }],
  "commands": []
}
