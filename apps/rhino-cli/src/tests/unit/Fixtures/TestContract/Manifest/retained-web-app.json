{
  "schema": "ose-test-contract-manifest-fixture/v1",
  "case": "a web application whose deploy tooling reads the manifest directly",
  "project": "widget-www",
  "owner": "widget-www",
  "group": "web-application",
  "disposition": "retained",
  "manifestPath": "apps/widget-www/package.json",
  "consumer": "the deployment platform reads engines and dependencies at install time",
  "requiredFields": ["name", "engines", "dependencies"],
  "verification": "npm install --workspace apps/widget-www --dry-run",
  "scripts": [{ "name": "postinstall", "command": "node ./scripts/link-assets.mjs" }],
  "commands": ["npx nx run widget-www:build"]
}
