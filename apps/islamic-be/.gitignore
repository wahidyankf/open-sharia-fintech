# Build output (regenerated by `nx run islamic-be:build`)
dist/

# Coverage profile (regenerated by `nx run islamic-be:test:unit`)
cover.out

# Local environment overrides; only .env.example is committed
.env
.env.*
!.env.example
