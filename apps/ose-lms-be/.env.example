# ose-lms-be — environment variable template
# Copy to .env.local for local development; never commit real secrets.

# OPTIONAL | u16 | Listener port (default: 8303)
# Format: this variable first, then the default; a bare PORT is not honoured
OSE_LMS_BE_PORT=
