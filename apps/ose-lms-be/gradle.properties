# Gradle daemon settings kept explicit so a CI runner and a developer machine behave identically.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.caching=true
org.gradle.parallel=false
