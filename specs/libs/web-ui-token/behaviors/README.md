# Behaviors — web-ui-token

Gherkin feature files for [web-ui-token](../../../../libs/web-ui-token/README.md), one folder per
capability.

```
specs/libs/web-ui-token/behaviors/
└── tokens/
    └── tokens-export.feature
```

## Consumption

No runner consumes these scenarios yet — `test:unit` is an `echo` placeholder for this package.
See the corpus [README.md](../README.md#status).
